print("Task Tracer App")
tasks = []
print("Task Tracer App with data support")
print(Task Tracer App - UI v1)
print("Task Tracker App - Main UI with Data")

def is_valid_task_name(name: str) -> bool:
    """
    Returns True if the given task name is valid.

    A valid task name is a non-empty string with at least
    one non-whitespace character. This helper function
    centralizes the validation logic so it can be reused
    in multiple parts of the application.
    """
    ...
